services:
  ddrawry-be:
    container_name: 'ddrawry-be'
    build:
      context: .
    ports:
      - '8000:8000'
    restart: always
    volumes:
      - ./log/gunicorn:/log
    env_file: 
      - .env  # .env 파일 전체 로드

